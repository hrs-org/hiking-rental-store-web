export interface RegisterStoreDto {
  name: string;
  description?: string;
  address?: string;
  phoneNumber?: string;
  firstName: string;
  lastName: string;
  email: string;
  password: string;
}
