export const apiPrefix = '/api';
export const authPrefix = `${apiPrefix}/auth`;

export const login = `${authPrefix}/login`;
export const register = `${authPrefix}/register`;
export const refreshToken = `${authPrefix}/refresh-token`;
